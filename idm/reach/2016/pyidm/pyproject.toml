[build-system]
requires = ["setuptools>=43.0.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "pyidm"
version = "0.0.1"
description = "HPS iDM Ana Helpers"
readme = "README.md"
requires-python = ">=3.6"
dependencies = []
